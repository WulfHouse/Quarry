# M9 Map Generic Test

import std.collections.map

fn main():
    # Use generic Map - should trigger built-in support in codegen
    var my_map: Map[int, int] = Map.new()
    my_map.insert(1, 2)
    let val = my_map.get(1)
    print("Value:", val)
    print("Success")
