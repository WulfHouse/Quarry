# Utils library - File and Path utilities

# File reading helper
fn read_file_content(path: String) -> String:
    # For MVP: placeholder - would use File.read_to_string via FFI
    return path

# Path helper
fn get_file_name(path: String) -> String:
    # For MVP: placeholder - would use Path operations via FFI
    return path
