fn factorial(n: int) -> int:
    if n <= 1:
        return 1
    return n * factorial(n - 1)

fn main():
    let x = 5
    let res = factorial(x)
    print("factorial of {x} is {res}")
    let emoji = "\u{1F600}"
    print(emoji)

